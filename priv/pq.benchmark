{code_paths, ["./ebin", "./deps/datum"]}.
{log_level,    info}.
{report_interval, 1}.
{driver,       pq_benchmark}.


%%
%% workload
{mode,            max}.
{duration,          1}.
{concurrent,      300}.

{operations, [
   {lease,   1}
   %,{suspend,      1}
]}.

%{pq_type,  disposable}. % worker use pattern
{pq_type,    reusable}. % worker use pattern
{pq_capacity,    1000}. % length of queue
%{pq_ondemand,    true}.
