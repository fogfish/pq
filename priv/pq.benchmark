{code_paths, ["./ebin", "./deps/feta"]}.
{log_level,    info}.
{report_interval, 1}.
{driver,       pq_benchmark}.


%%
%% workload
{mode,            max}.
{duration,          1}.
{concurrent,      300}.

{operations, [
    {lease,   100000}
   %,{suspend,      1}
]}.

{pq_type,  disposable}. % worker use pattern
%{pq_type,    reusable}. % worker use pattern
{pq_capacity,     100}. % length of queue
{pq_lifecycle,   1000}. % life cycle of test process (ms)
%{pq_ondemand,    true}.
